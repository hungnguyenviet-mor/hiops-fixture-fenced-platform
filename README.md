# hiops fixture: fenced platform Dockerfile

One-file public fixture for epic #841 (repo onboarding / toolchain amendment) end-to-end runs.
Source of truth: `backend/tests/fixtures/repo_fixtures/fenced_platform/Dockerfile` in the hiops repo.
